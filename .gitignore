# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables - NEVER commit these!
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build outputs
*.tsbuildinfo

# Screenshots (keep screenshots but ignore temp files)
# screenshots/*.png  # Uncomment to ignore screenshots

# Local reference files (not for repo)
GITHUB_TOPICS.md


# Android signing keystore (do not commit)
android/app/release-keystore.jks

# --- Wikiflix custom ignores ---
# Tool/binary artifacts
bundletool-all-*.jar
aab-manifest.txt

# Capacitor/Android build outputs (redundant if in android/.gitignore, but safe)
android/app/build/
android/app/outputs/
android/build/
android/outputs/

# Local Capacitor config (if not meant for repo)
capacitor.config.ts

# Python venv (if present in root)
streaming_pilot_venv/

# Miscellaneous
*.bak
*.tmp
*.swp
*.orig
*.rej

# End Wikiflix custom ignores

